import React from 'react';
import Logo from '../composants/Logo';
import Navigation from '../composants/Navigation';

const Home = () => {
    return (
        <div>
            <Logo />
            <Navigation />
        </div>
    );
};

export default Home;